/* =========================
   STUDIO LAYOUT
   ========================= */

.StudioContainer {
  width: 100%;
  background: #242421;
  border-radius: 15vw;
  margin-top: 20vw;
}

.StudioIntro {
  width: 100%;
}

.StudioGrid {
  width: 100%;
  margin: 5vw;
}

/* =========================
   MUURI GRID WRAPPER
   ========================= */

.grid {
  position: relative;
  margin: 0;
  min-height: 100vh;
  /* background: blue; */
}

.item {
  display: block;
  position: absolute;      /* required by Muuri */
  width: 26.5vw;
  margin: 2vw;
  z-index: 1;
  overflow: hidden;        /* for overlay clipping */
  /* border-radius: 16px; */
}

.item-label {
  /* background: blue; */
}

.item.muuri-item-dragging { z-index: 3; }
.item.muuri-item-releasing { z-index: 2; }
.item.muuri-item-hidden { z-index: 0; }

.item-content {
  position: relative;
  width: 100%;
  height: 100%;
  overflow: hidden;
}

/* If you're using <img class="item__img"> you can remove this selector. */
.item img {
  border-radius: 10px; /* optional; note this differs from .item's 16px */
}

/* =========================
   ITEM IMAGE
   ========================= */

.item__img {
  width: 100%;
  object-fit: cover;
  display: block;
  transform: scale(1.02);
  transition: transform 200ms ease;
}

/* =========================
   ITEM OVERLAY
   ========================= */

.item__overlay {
  position: absolute;
  inset: 0;
  display: grid;
  align-items: end;
  padding: 16px;
  background: linear-gradient(
    to top,
    rgba(0,0,0,0.75),
    rgba(0,0,0,0.15) 60%,
    rgba(0,0,0,0)
  );

  opacity: 0;
  transform: translateY(6px);

  transition: opacity 200ms ease, transform 200ms ease;
}

/* =========================
   OVERLAY CONTENT
   ========================= */

.item__content {
  color: white;
  display: grid;
  gap: 10px;
}

.item__title {
  margin: 0;
  font-size: 1.1rem;
  line-height: 1.2;
}

.item__desc {
  margin: 0;
  opacity: 0.9;
  font-size: 0.95rem;
}

/* =========================
   BUTTONS
   ========================= */

.item__actions {
  display: flex;
  gap: 10px;
  flex-wrap: wrap;
}

.btn {
  appearance: none;
  border: 1px solid rgba(255,255,255,0.35);
  background: rgba(255,255,255,0.18);
  color: white;

  padding: 8px 10px;
  border-radius: 999px;

  text-decoration: none;
  font-size: 0.9rem;
  line-height: 1;

  cursor: pointer;

  backdrop-filter: blur(8px);
}

.btn--ghost {
  background: transparent;
}

/* =========================
   INTERACTIONS
   ========================= */

.item:hover .item__overlay,
.item:focus-within .item__overlay {
  opacity: 1;
  transform: translateY(0);
  cursor: pointer;
}

.item:hover .item__img,
.item:focus-within .item__img {
  transform: scale(1.06);
}

/* =========================
   ACCESSIBILITY
   ========================= */

@media (prefers-reduced-motion: reduce) {
  .item__img,
  .item__overlay {
    transition: none;
  }
}
